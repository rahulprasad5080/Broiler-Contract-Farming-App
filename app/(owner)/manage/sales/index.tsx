import { SalesEntryScreen } from '@/components/SalesEntryScreen';

export default function OwnerSalesScreen() {
  return (
    <SalesEntryScreen
      title="Sales Finalization"
      subtitle="Owner can record sales, enter final rate per kg and finalize revenue."
    />
  );
}
