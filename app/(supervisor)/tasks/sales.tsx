import { SalesEntryScreen } from '@/components/screens';

export default function SupervisorSalesScreen() {
  return (
    <SalesEntryScreen
      title="Supervisor Sales Entry"
      subtitle="Record sale quantities for any supervised active farm. Rate remains owner only."
    />
  );
}
