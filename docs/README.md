# Project Docs

API reference files from the backend team live here so the repository root stays focused on app code and configuration.

Use `npm run docs:extract` to extract `docs/new-api.docx` into `docs/extracted-api.txt` for quick searching.
